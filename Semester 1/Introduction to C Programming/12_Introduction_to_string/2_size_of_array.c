#include<stdio.h>
int main()
{
    // int a[5];
    // int sz=sizeof(a)/sizeof(int);
    // printf("int \t%d\n",sz);

    // float f[5];
    // int sz=sizeof(f)/sizeof(float);
    // printf("float \t%d",sz);

    // double f[5];
    // int sz=sizeof(f)/sizeof(double);
    // printf("double \t%d",sz);

    char c[5];
    int sz=sizeof(c)/sizeof(char);
    printf("char \t%d",sz);
}